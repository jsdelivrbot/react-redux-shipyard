import React, { Component } from 'react';
import ShipList from "../containers/ship-list";

export default class App extends Component {
  render() {
    return (
      <div>
          <ShipList />
      </div>
    );
  }
}
